import { AdminOrderByPipe } from './admin-order-by.pipe';

describe('AdminOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
